# Set default behavior to automatically normalize line endings
* text=auto

# Python source files
*.py text diff=python

# Shell scripts should use LF
*.sh text eol=lf
docker-run.sh text eol=lf

# PowerShell scripts
*.ps1 text eol=crlf
docker-run.ps1 text eol=crlf

# Batch scripts
*.bat text eol=crlf
*.cmd text eol=crlf

# Docker files
Dockerfile text
*.Dockerfile text
docker-compose.yml text
docker-compose.yaml text

# JSON files
*.json text

# YAML files
*.yml text
*.yaml text

# Jupyter notebooks
*.ipynb text

# Large model files - uncomment and use Git LFS if needed
# *.h5 filter=lfs diff=lfs merge=lfs -text
# *.pt filter=lfs diff=lfs merge=lfs -text
# *.pth filter=lfs diff=lfs merge=lfs -text
# *.onnx filter=lfs diff=lfs merge=lfs -text

# Documentation
*.md text
*.txt text
LICENSE text
README text

# Graphics
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.svg text

# Exclude files from exporting
.gitattributes export-ignore
.gitignore export-ignore
.github export-ignore 